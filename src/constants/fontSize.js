import { RFValue } from '../utils/resolutions';

const STANDARD_SCREEN_HEIGHT = 720;

export const tiny = RFValue(9, STANDARD_SCREEN_HEIGHT);

export const small = RFValue(10, STANDARD_SCREEN_HEIGHT);

export const normal = RFValue(15, STANDARD_SCREEN_HEIGHT);

export const large = RFValue(18, STANDARD_SCREEN_HEIGHT);

export const larger = RFValue(20, STANDARD_SCREEN_HEIGHT);

export const biggest = RFValue(28, STANDARD_SCREEN_HEIGHT);
